git:
  overrideGpg: true
  pagers:
    - colorArg: always
      pager: delta --dark --paging=never --syntax-theme base16-256 -s
gui:
  nerdFontsVersion: "3"
  expandFocusedSidePanel: true
os:
  edit: "nvim"
  open: "open"
