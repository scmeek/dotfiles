" inoremp = i (insert) + nore (no, recurssive calling IMPORTANT) + map

inoremap jj <Esc> " Go back to normal mode

" map <leader> to space
"nnoremap <SPACE> <Nop>
"let mapleader = " "

let mapleader = "," " map <leader> to comma


" save, similar to CMD+S
noremap <leader>s :w<CR>

