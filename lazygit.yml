quitOnTopLevelReturn: true
gui:
  showIcons: true
  theme:
    selectedLineBgColor:
      - black
    unstagedChangesColor:
      - yellow
  sidePanelWidth: 0.2
git:
  paging:
    colorArg: always
    pager: delta --paging=never --line-numbers --side-by-side --dark
