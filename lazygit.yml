quitOnTopLevelReturn: true
gui:
  showIcons: true
  theme:
    selectedLineBgColor:
      - black
    unstagedChangesColor:
      - yellow
git:
  paging:
    colorArg: always
    pager: delta --paging=never --line-numbers --dark
